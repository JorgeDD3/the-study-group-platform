import { PageShell } from "@/components/layout/page-shell";

export default function ClientPage() {
  return (
    <PageShell
      eyebrow="Client"
      title="Client Dashboard"
      description="Student bookings, session history, and account information will live here."
    />
  );
}
