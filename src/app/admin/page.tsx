import { PageShell } from "@/components/layout/page-shell";

export default function AdminPage() {
  return (
    <PageShell
      eyebrow="Admin"
      title="Admin Dashboard"
      description="Internal tools for managing users, tutors, sessions, organizations, and reports will live here."
    />
  );
}
